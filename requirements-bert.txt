# Optional dependencies for the BERT sentiment backend (SENTIMENT_BACKEND=bert).
# Heavy (pulls in torch); kept out of the default install. Enable with:
#   pip install -r requirements-bert.txt
# Without these, the sentiment backend transparently falls back to TextBlob.
transformers>=4.40,<5.0
torch>=2.2,<3.0
