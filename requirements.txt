# Full API-deployment stack. The library-surface dependencies (cats.lite +
# cats.calibration) are pinned in pyproject.toml — keep the shared ranges
# (pydantic, pydantic-settings, spacy, textblob, structlog, httpx) in sync
# with [project.dependencies] there.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
redis>=5.0,<6.0
cryptography>=42.0,<50.0
spacy>=3.7,<4.0
textblob>=0.18,<1.0
apscheduler>=3.10,<4.0
structlog>=24.1,<25.0
prometheus-client>=0.20,<1.0
httpx>=0.27,<1.0
alembic>=1.13,<2.0
